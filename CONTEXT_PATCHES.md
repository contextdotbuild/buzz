# Context-carried patches

This file mirrors the carried-patch ledger in the Back Office corpus. It records which changes `contextdotbuild/buzz` still carries outside an official Buzz release and how to decide whether each patch can be retired. Keep each product change independently reviewable and upstreamable; do not turn this fork into a permanent divergence branch.

Last reconciled: 2026-08-28.

| Slice | Current state | Fork branch or commit | Upstream state | Installed state | Retirement rule |
| --- | --- | --- | --- | --- | --- |
| Maximum-depth-one managed-agent replies | Live and proved on all nine canonical iMac runtimes, including the normal MCP publishing path | Runtime source `d10de1f346242cf2615026764671bb2e73f577e8`; cutover contract `ec9b40ecc886cd36cbe885babbf8485532d2ff5f`; original standalone-CLI slice `784a021a7554c65f20a4fb0bbff81011ed3c911b` | No equivalent accepted upstream patch recorded | Immutable iMac ACP/MCP/CLI runtime `d10de1f...`; exact child-target reply `DEPTH-ONE-828A`; official signed Buzz.app unchanged | Drop only after an official runtime canonicalizes child targets across its managed-agent MCP and CLI paths and the same live proof passes |
| Existing remote agent in DM mentions | Live and proved on the paired no-keyring Context build | `fix/existing-agent-mentions` at `dcf7dbed85940d520fa5c8035b46def916c50f79`; integration commit `1507c63ccd4ae7a1291915d81a7d7a2aad747ca5` | Draft upstream PR [#6882](https://github.com/block/buzz/pull/6882) | Installed in the isolated MacBook Context app; exact reply `FINAL-CONTEXT-826C`; zero MacBook runtimes | Drop only after an official released client passes the same existing-DM mention proof |
| Managed-agent startup catch-up | Implemented but not live on the iMac runtime owner | `fix/restart-replay` at `b58426b86b991c8ff03d0d2e4055183410cf69d3`; integration commits `d8eea6fa06e35c41b8128738e6eabdb77b2e2868` and `aa53452da454b4f378810d4edc98d4322edaeb7b` | Upstream PR [#6772](https://github.com/block/buzz/pull/6772) was open and DCO-failing when last checked | Included in MacBook Context but ineffective there because the MacBook owns no standing runtime | Carry source without claiming protection until a reviewed iMac installation and real restart-gap proof exist |
| Add an existing agent to a channel without a local runtime | Implemented and focused-E2E proved; live paired replacement proof pending | `fix/add-existing-channel-agent` at `a6cc28103a509d60d864e2810dd82a19c7dfe6e7`; integration commit `f85704b41d37993658b83dda6293dd7c4b2ccecf` | No matching accepted upstream patch recorded | Installed in the isolated MacBook Context app; official Buzz.app unchanged | Drop only after an official released client can attach an existing public-key identity with zero local runtimes |
| Reject empty or whitespace-only message publication | Live and proved on all nine canonical iMac runtimes; media-only messages remain valid | `fix/reject-empty-messages` at `0fe6a54b28195be7e2a188f800a0427b7b383513`; cutover contract `765cbecfc` | No matching accepted upstream patch recorded | Included in the immutable iMac runtime family; official Buzz.app and global CLI unchanged | Drop only after an official released CLI rejects empty stdin before relay publication while preserving media-only sends |
| Hide archived identities on mobile | Observed upstream only; not locally carried | None | Draft upstream PR [#5359](https://github.com/block/buzz/pull/5359) when last checked | None | Recheck the next official mobile release; carry only if still needed and unavailable upstream |

For each official Buzz update, record its tag and source commit, compare every row, classify the slice as **drop**, **keep**, or **rebase**, run the focused tests for that slice, and perform one relevant live proof before changing the installed state.
